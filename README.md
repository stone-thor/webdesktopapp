# HTML 5 App Module in Python using pywebview

## run application 
`$ python webdesktopapp.py <plugin_name>`

see folder `./res` for plugins. name of folder is the `plugin_name`.

## run unittests
`$ python -m unittest ./test/applauncher/test_applauncher.py`